diff --git a/hw/arm/apple-silicon/t8030.c b/hw/arm/apple-silicon/t8030.c
index 565f02e..8b2045b 100644
--- a/hw/arm/apple-silicon/t8030.c
+++ b/hw/arm/apple-silicon/t8030.c
@@ -2447,4 +2447,6 @@ static void t8030_create_aop(AppleT8030MachineState *t8030)
-    // sbd = apple_aop_audio_create(APPLE_AOP(aop));
-    // assert_nonnull(sbd);
-    // object_property_add_child(OBJECT(aop), "aop-audio", OBJECT(sbd));
-    // sysbus_realize_and_unref(sbd, &error_fatal);
+    if (t8030->aop_audio) {
+        sbd = apple_aop_audio_create(APPLE_AOP(aop));
+        assert_nonnull(sbd);
+        object_property_add_child(OBJECT(aop), "aop-audio", OBJECT(sbd));
+        sysbus_realize_and_unref(sbd, &error_fatal);
+    }
@@ -3061,0 +3064 @@ PROP_GETTER_SETTER(bool, force_dfu);
+PROP_GETTER_SETTER(bool, aop_audio);
@@ -3128,0 +3132,4 @@ static void t8030_class_init(ObjectClass *klass, const void *data)
+    object_class_property_add_bool(klass, "aop-audio", t8030_get_aop_audio,
+                                   t8030_set_aop_audio);
+    object_class_property_set_description(klass, "aop-audio",
+                                          "Enable experimental AOP audio");
diff --git a/include/hw/arm/apple-silicon/t8030.h b/include/hw/arm/apple-silicon/t8030.h
index 3e53c81..b6bbde6 100644
--- a/include/hw/arm/apple-silicon/t8030.h
+++ b/include/hw/arm/apple-silicon/t8030.h
@@ -73,0 +74 @@ typedef struct {
+    bool aop_audio;
diff --git a/hw/audio/apple-silicon/aop-audio.c b/hw/audio/apple-silicon/aop-audio.c
index 5b61a11..5a9f77d 100644
--- a/hw/audio/apple-silicon/aop-audio.c
+++ b/hw/audio/apple-silicon/aop-audio.c
@@ -101,7 +101,12 @@ type_init(apple_aop_audio_register_types);
 // - apac | AOP Audio Controller?
 static const uint32_t apple_aop_devices[] = {
     'edtC', 'acmm', 'aphc', 'lpfw', 'leap', 'aphd', 'aph ',
-    'ahdc', 'pcmM', 'lpai', 'mca0', 'mca1', 'apac',
+    /*
+     * Do not advertise lpai yet. This endpoint reports zero IO handlers, and
+     * iOS 14 can spin waiting for an input buffer on the missing lpai handler.
+     */
+    'ahdc', 'pcmM', 'mca0', 'mca1', 'apac',
 };

 static AppleAOPResult apple_aop_audio_get_prop(void *opaque, uint32_t prop,
@@ -146,14 +151,18 @@ static AppleAOPResult apple_aop_audio_handle_command(void *opaque, uint16_t seq,

     switch (ldl_le_p(payload + sizeof(uint32_t))) {
-    case COMMAND_GET_DEVICE_ID:
+    case COMMAND_GET_DEVICE_ID: {
+        uint32_t device_index = ldl_le_p(payload + COMMAND_HDR_LEN);
+
         AOP_DPRINTF("AOPAudio GetDeviceID %d",
-                    ldl_le_p(payload + COMMAND_HDR_LEN));
+                    device_index);

-        stl_le_p(payload_out,
-                 apple_aop_devices[ldl_le_p(payload + COMMAND_HDR_LEN)]);
+        if (device_index >= ARRAY_SIZE(apple_aop_devices)) {
+            return AOP_RESULT_ERROR;
+        }
+        stl_le_p(payload_out, apple_aop_devices[device_index]);
         break;
+    }
     case COMMAND_GET_DEVICE_PROP:
         AOP_DPRINTF("AOPAudio GetDeviceProperty %X 0x%X",
                     ldl_le_p(payload + COMMAND_HDR_LEN),
